export const metadata = {
  title: "DashBoard shop Auth",
  description: "DashBoard shop Authentication",
};

export default function DashBoardLayout({ children }: any) {
  return <section className="auth-layout">{children}</section>;
}
