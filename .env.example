# Server-side URL for users, reviews, and todos.
USERS_API_URL=http://localhost:4000

# Generate a strong random value and copy the same value to backend/.env.
INTERNAL_API_SECRET=replace-with-a-strong-random-secret
